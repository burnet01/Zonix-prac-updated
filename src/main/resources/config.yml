mongo:
  # MongoDB connection URI (takes priority over host/port/auth below).
  # Example (local):    "mongodb://username:password@127.0.0.1:27017"
  # Example (Atlas):    "mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?appName=Cluster0"
  # Leave empty or set to "none" to use host/port/authentication settings.
  uri: ""
  database: "practice"
  # Fallback used when uri is empty:
  host: "127.0.0.1"
  port: 27017
  authentication:
    enabled: false
    username: "admin"
    password: "dev"
    database: "admin"
Release-Day: "March 00th"
potion:
  splash: 0.15
region-lock: true
allowed-continents:
  - 'EU'
  - 'SA'
  - 'NA'
  - 'OC'
  - 'AS'
  - 'AF'
  - 'AN'